<main>
  <h1>{{name}}</h1>
  <img class="cube" src="{{imageUrl}}">
  <div class="details">
    <p><span>Description:</span> {{description}}</p>
    <p><span>Difficulty level:</span> {{difficulty}}</p>
    <div class="btns">
      <a class="btn" href="/">Back</a>
      <a class="btn btn-del" href="/delete/{{id}}">Delete</a>
    </div>
  </div>
</main>